services:
  api:
    build:
      context: ../
      dockerfile: ./backend/Dockerfile
    volumes:
      - ../:/app
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ../
      dockerfile: ./frontend/Dockerfile
    volumes:
      - ../:/app
    ports:
      - "3000:3000"